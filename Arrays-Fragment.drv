#if ! #keyclass(Object) && ! #keyclass(Reference)

/** A static, final, empty type-specific array. */
public final static KEY_TYPE[] KEY_EMPTY_ARRAY = new KEY_TYPE[ 0 ];

#endif

// Local Variables:
// mode: jde
// tab-width: 4
// End:
