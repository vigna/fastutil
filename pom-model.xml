<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>@GROUP_ID@</groupId>
  <artifactId>fastutil</artifactId>
  <packaging>jar</packaging>
  <name>fastutil (Luxonit fork)</name>
  <version>@VERSION@</version>
  <description>Luxonit&apos;s fork of fastutil. It extends the Java Collections Framework by providing type-specific maps, sets, lists, and queues with a small memory footprint and fast access and insertion; it also provides big (64-bit) arrays, sets and lists, sorting algorithms, fast, practical I/O classes for binary and text files, and facilities for memory mapping large files. Note that if you have both this jar and fastutil-core.jar in your dependencies, fastutil-core.jar should be excluded.</description>
  <url>https://github.com/luxonit/fastutil</url>
  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.html</url>
      <distribution>repo</distribution>
    </license>
  </licenses>
  <scm>
    <connection>scm:git:https://github.com/luxonit/fastutil.git</connection>
    <developerConnection>scm:git:https://github.com/luxonit/fastutil.git</developerConnection>
    <url>https://github.com/luxonit/fastutil</url>
  </scm>
  <developers>
    <developer>
      <id>luxonit</id>
      <name>Luxonit s.r.o.</name>
      <organization>Luxonit s.r.o.</organization>
    </developer>
    <developer>
      <id>vigna</id>
      <name>Sebastiano Vigna</name>
      <email>sebastiano.vigna@unimi.it</email>
    </developer>
  </developers>
  <properties>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
  </properties>
  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
