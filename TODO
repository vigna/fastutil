- Run tests for new fully indirect heap-based queues.
- Stale references to nodes might remain in dirNode variables in tree-based sets
- What do we do with toArray()?
- Check that type casts for keySet() in Maps do work
- Explain which interfaces have "Object" in front and which not.
